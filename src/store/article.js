import {ADD_ARTICLE} from '../common/constants'

/* export const addArticle = (state={}, action={}) => {
    switch(action.type){
        case ADD_ARTICLE:
            return {

            }
    }
} */